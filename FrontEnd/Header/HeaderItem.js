export default class HeaderItem {
  constructor(logo, image, sideDrawer, rightArrow) {
    this.logo = logo;
    this.image = image;
    this.sideDrawer = sideDrawer;
    this.rightArrow = rightArrow;
  }
}
